import { useState } from 'react';import api from '../services/api';
export default function UserRegistrationForm(){const [f,sf]=useState({name:'',registrationNumber:'',marks:'',address:'',email:'',password:''});const [e,se]=useState({});
const v=()=>{const x={};if(!f.name.trim())x.name='Required';if(!f.registrationNumber.trim())x.registrationNumber='Required';if(!f.address.trim())x.address='Required';if(!/^\d+(\.\d+)?$/.test(f.marks))x.marks='Numeric only';if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(f.email))x.email='Invalid email';if(!/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[^A-Za-z\d]).{8,}$/.test(f.password))x.password='Weak password';return x;};
const submit=async ev=>{ev.preventDefault();const errs=v();se(errs);if(Object.keys(errs).length)return;console.log('Student Form Data:',f);await api.post('/students',{...f,marks:Number(f.marks)});sf({name:'',registrationNumber:'',marks:'',address:'',email:'',password:''});};
return <form className='card' onSubmit={submit}><h3>User Registration Form</h3>{Object.keys(f).map(k=><div key={k}><input placeholder={k} type={k==='password'?'password':'text'} value={f[k]} onChange={ev=>sf({...f,[k]:ev.target.value})}/>{e[k]&&<small>{e[k]}</small>}</div>)}<button>Submit</button></form>}
