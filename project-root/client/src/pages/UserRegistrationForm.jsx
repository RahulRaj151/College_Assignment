import { useState } from 'react';import Navbar from '../components/Navbar';import Sidebar from '../components/Sidebar';import api from '../services/api';
export default function UserRegistrationForm(){const [f,sf]=useState({name:'',registrationNumber:'',marks:'',address:'',email:'',password:''});const [err,setErr]=useState({});const [msg,setMsg]=useState('');
const validate=()=>{const e={};if(!f.name.trim())e.name='Name required';if(!f.registrationNumber.trim())e.registrationNumber='Registration number required';if(!f.address.trim())e.address='Address required';if(!/^\d+(\.\d+)?$/.test(f.marks))e.marks='Marks must be numeric';if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(f.email))e.email='Invalid email';if(!/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[^A-Za-z\d]).{8,}$/.test(f.password))e.password='Need upper/lower/number/special';return e;};
const submit=async(e)=>{e.preventDefault();const v=validate();setErr(v);if(Object.keys(v).length)return;try{await api.post('/students/register',{...f,marks:Number(f.marks)});console.log('Student data:',f);setMsg('Student saved');sf({name:'',registrationNumber:'',marks:'',address:'',email:'',password:''});}catch(ex){setMsg(ex.response?.data?.message||'Failed');}};
return <main className='p-4 md:p-8'><Navbar/><div className='grid md:grid-cols-[220px_1fr] gap-4'><Sidebar/><form onSubmit={submit} className='glass p-6 space-y-3'><h2 className='text-2xl font-bold'>Student Registration Form</h2>{Object.keys(f).map(k=><div key={k}><input className='input' type={k==='password'?'password':'text'} placeholder={k} value={f[k]} onChange={e=>sf({...f,[k]:e.target.value})}/>{err[k]&&<p className='text-rose-300 text-sm mt-1'>{err[k]}</p>}</div>)}<button className='btn-primary'>Submit</button>{msg&&<p>{msg}</p>}</form></div></main>}
