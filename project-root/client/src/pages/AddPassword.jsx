import { useState } from 'react';import api from '../services/api';
const gen=()=>Math.random().toString(36).slice(-12)+'@A1';
export default function AddPassword(){const auth=JSON.parse(localStorage.getItem('auth')||'{}');const [show,setShow]=useState(false);const [form,setForm]=useState({website:'',username:'',password:''});const[msg,setMsg]=useState('');
const submit=async e=>{e.preventDefault();try{await api.post('/password/add',{...form,userId:auth.userId});setMsg('Saved');setForm({website:'',username:'',password:''});}catch{setMsg('Error');}};
return <form className='card' onSubmit={submit}><h3>Add Password</h3><input placeholder='website' value={form.website} onChange={e=>setForm({...form,website:e.target.value})}/><input placeholder='username' value={form.username} onChange={e=>setForm({...form,username:e.target.value})}/><div className='row'><input type={show?'text':'password'} placeholder='password' value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button type='button' onClick={()=>setShow(!show)}>{show?'Hide':'Show'}</button><button type='button' onClick={()=>setForm({...form,password:gen()})}>Generate</button></div><button>Save</button><p>{msg}</p></form>}
